# 🚀 دليل البدء السريع - نظام الكشف عن سرطان الرئة

## ⚡ البدء في 5 دقائق

### 1️⃣ التثبيت (دقيقة واحدة)

```bash
# استنساخ المشروع أو تنزيله
cd Lung_cancer_detector

# تثبيت المكتبات
pip install -r requirements.txt
```

### 2️⃣ توليد البيانات (30 ثانية)

```bash
# توليد 40 صورة تجريبية (20 سليمة + 20 مصابة)
python generate_data_enhanced.py
```

### 3️⃣ تدريب النموذج (دقيقتان)

**اختر أحد النموذجين:**

```bash
# نموذج PyTorch (أفضل دقة)
python train_model.py

# أو نموذج Scikit-learn (أسرع)
python train_improved.py
```

### 4️⃣ تشغيل التطبيق (10 ثواني)

```bash
# تطبيق Streamlit المحسّن (موصى به)
streamlit run app_enhanced.py

# أو التطبيق الأساسي
streamlit run app.py

# أو نسخة Sklearn
streamlit run app_sklearn.py
```

**أو استخدم ملف التشغيل السريع (Windows):**
```bash
run.bat
```

---

## 📋 قائمة التحقق السريعة

- [ ] تثبيت Python 3.8+
- [ ] تثبيت المكتبات: `pip install -r requirements.txt`
- [ ] توليد البيانات: `python generate_data_enhanced.py`
- [ ] تدريب النموذج: `python train_model.py`
- [ ] اختبار النموذج: `python test_model.py`
- [ ] تشغيل التطبيق: `streamlit run app_enhanced.py`

---

## 🎯 الأوامر الأساسية

### توليد البيانات
```bash
python generate_data.py              # بيانات أساسية (30 صورة)
python generate_data_enhanced.py     # بيانات محسّنة (40 صورة)
```

### التدريب
```bash
python train_model.py                # PyTorch CNN
python train_model_sklearn.py       # Sklearn - أساسي
python train_improved.py             # Sklearn - محسّن
```

### الاختبار
```bash
python test_model.py                 # اختبار شامل مع رسوم بيانية
python config.py                     # عرض الإعدادات
```

### التشغيل
```bash
streamlit run app.py                 # تطبيق PyTorch أساسي
streamlit run app_enhanced.py        # تطبيق PyTorch محسّن ⭐
streamlit run app_sklearn.py        # تطبيق Sklearn
```

### واجهة HTML
```bash
# افتح الملف مباشرة في المتصفح
index.html
```

---

## 🔧 حل المشاكل الشائعة

### ❌ المكتبات غير موجودة
```bash
pip install -r requirements.txt
```

### ❌ النموذج غير موجود
```bash
# تأكد من وجود مجلد img وصور بداخله
python generate_data_enhanced.py

# ثم درّب النموذج
python train_model.py
```

### ❌ خطأ في CUDA/GPU
```bash
# استخدام CPU فقط (يعمل تلقائياً)
# النموذج يكتشف GPU تلقائياً ويستخدم CPU إذا لم يتوفر
```

### ❌ خطأ في Streamlit
```bash
# تحديث Streamlit
pip install --upgrade streamlit

# أو إعادة التثبيت
pip uninstall streamlit
pip install streamlit==1.28.1
```

---

## 📊 البنية السريعة

```
Lung_cancer_detector/
├── 📄 run.bat                  ← ابدأ من هنا (Windows)
├── 📖 README.md                ← الدليل الكامل
├── ⚡ QUICKSTART.md            ← هذا الملف
│
├── 🧠 النماذج
│   ├── app_enhanced.py         ← الأفضل ⭐
│   ├── app.py
│   └── app_sklearn.py
│
├── 🎓 التدريب
│   ├── train_model.py          ← PyTorch
│   └── train_improved.py       ← Sklearn ⭐
│
├── 🧪 الاختبار
│   └── test_model.py           ← اختبار شامل
│
├── 🎨 البيانات
│   ├── generate_data_enhanced.py  ← الأفضل ⭐
│   └── generate_data.py
│
└── 📦 الإعدادات
    ├── config.py               ← التكوين المركزي
    ├── requirements.txt
    └── .gitignore
```

---

## 💡 نصائح سريعة

### 🚀 للبدء السريع
استخدم **run.bat** على Windows - قائمة تفاعلية لجميع الخيارات

### 🎯 للحصول على أفضل النتائج
1. استخدم `generate_data_enhanced.py` لصور أفضل
2. درّب بـ `train_improved.py` (Sklearn - سريع ودقيق)
3. شغّل `app_enhanced.py` لأفضل واجهة

### 📈 لزيادة الدقة
- استخدم المزيد من الصور الفعلية
- زد عدد العصور في التدريب
- استخدم Data Augmentation

### ⚡ للأداء الأسرع
- استخدم نموذج Sklearn بدلاً من PyTorch
- قلل حجم الصور
- استخدم GPU إذا كان متاحاً

---

## 🎓 موارد إضافية

- 📖 [README.md](README.md) - الدليل الكامل
- 📝 [CHANGELOG.md](CHANGELOG.md) - سجل التحديثات
- ⚙️ [config.py](config.py) - الإعدادات والتكوين
- 🧪 [test_model.py](test_model.py) - كود الاختبار

---

## 📞 المساعدة

هل واجهت مشكلة؟

1. راجع [README.md](README.md#-الأسئلة-الشائعة)
2. تحقق من [CHANGELOG.md](CHANGELOG.md)
3. افتح Issue على GitHub
4. راسل المطور

---

## ✅ الخطوات التالية بعد التثبيت

1. ✨ جرّب التطبيق مع الصور التجريبية
2. 📊 راجع نتائج الاختبار
3. 🔧 عدّل الإعدادات في `config.py`
4. 📸 أضف صورك الخاصة
5. 🎓 درّب النموذج على بياناتك
6. 🚀 شارك المشروع!

---

<div align="center">

**🫁 صُنع بـ ❤️ للمساعدة في الكشف المبكر عن سرطان الرئة**

[⭐ ضع نجمة على GitHub](https://github.com) | [📧 تواصل معنا](mailto:fahad@example.com)

</div>
