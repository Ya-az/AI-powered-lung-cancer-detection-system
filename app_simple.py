import os
import streamlit as st
from PIL import Image
import numpy as np

# ===== تكوين الصفحة =====
st.set_page_config(
    page_title="تصنيف أمراض الرئة",
    page_icon="🫁",
    layout="centered"
)

st.title("🫁 تحليل صور الرئة")
st.subheader("نموذج ذكاء اصطناعي للكشف عن سرطان الرئة")

# ===== واجهة الرفع =====
st.info("⚙️ جاري تثبيت المكتبات... سيتم تحديث هذه الصفحة عندما يكون كل شيء جاهزاً")

st.markdown("---")
st.markdown("### 📋 الخطوات المتبقية:")
st.write("""
1. ✓ تم إنشاء ملفات المشروع
2. ⏳ جاري تثبيت المكتبات (torch, torchvision, streamlit...)
3. ⏳ نسخ الصور إلى مجلد img/
4. ⏳ تدريب النموذج
5. ⏳ تشغيل التطبيق النهائي

**المكتبات المطلوبة:**
""")

code_block = """
pip install torch torchvision Pillow numpy streamlit
"""
st.code(code_block, language="bash")

st.markdown("---")
st.markdown("### 📝ملاحظة:")
st.write("""
إذا كنت تواجه مشاكل في التثبيت، يمكنك:
- استخدام `conda install pytorch torchvision -c pytorch`
- أو استخدام نموذج pre-trained بدلاً من التدريب من الصفر
""")
